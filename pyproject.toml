[project]
name = "python"
version = "0.1.0"
description = "All dependencies associated with Exercism's Python track"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.0.3",
    "pytest-cache>=1.0",
    "pytest-subtests>=0.15.0",
]
